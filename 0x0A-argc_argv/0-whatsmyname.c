#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * @argc : count argument
 * @argv : array of pointer
 * Return : always 0
 *
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
