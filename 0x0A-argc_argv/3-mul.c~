#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * main -entry point
 * @argc : argument count
 * @argv : array of pointer to the string
 * Return : always 0
 *
 */
int main(int argc, char *argv[])
{
	int multi;
	int i, j;

	if (argc == 3)
	{
		for (i = 1; i < argc; i++)
		{
			for (j = 1; j < argc; j++)
			{
				printf("%d\n", atoi(argv[j]) * atoi(argv[i]));
				return (0);
			}
		}
	}
	else
	{
		printf("Error\n");
		return (-1);
	}
}
